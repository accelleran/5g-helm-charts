apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acc-5g-cu-cp.fullname" . }}-test-connection"
  labels:
    {{- include "acc-5g-cu-cp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acc-5g-cu-cp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
