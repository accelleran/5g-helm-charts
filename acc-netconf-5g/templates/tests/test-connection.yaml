apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "five-g-appl.fullname" . }}-test-connection"
  labels:
    {{- include "five-g-appl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "five-g-appl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
