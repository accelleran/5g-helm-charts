apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amfControllerAppl.fullname" . }}-test-connection"
  labels:
    {{- include "amfControllerAppl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amfControllerAppl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
